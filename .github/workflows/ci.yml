name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-20.04
    env:
      POETRY_HOME: "${{ github.workspace }}/.cache/poetry"
      POETRY_CACHE_DIR: "${{ github.workspace }}/.cache/pypoetry"
    steps:
    - uses: actions/checkout@v2
    - uses: actions/cache@v2
      with:
        key: poetry-ubuntu-20.04-py3.6-${{ hashFiles('poetry.lock') }}
        path: |
          .cache/poetry
          .cache/pypoetry
    - uses: actions/setup-python@v2
      with:
        python-version: '3.6'
    - uses: Gr1N/setup-poetry@v7
    - name: Setup virtualenv
      run: poetry install
    - name: Lint
      run: make -k -j4 lint
  test:
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        os: ['ubuntu-20.04', "windows-2019", "macos-10.15"]
        python: ['3.6', '3.7', '3.8', '3.9', '3.10']
    runs-on: ${{ matrix.os }}
    env:
      POETRY_HOME: "${{ github.workspace }}/.cache/poetry"
      POETRY_CACHE_DIR: "${{ github.workspace }}/.cache/pypoetry"
    steps:
    - uses: actions/checkout@v2
    - uses: actions/cache@v2
      with:
        key: poetry-${{ matrix.os }}-py${{ matrix.python }}-${{ hashFiles('poetry.lock') }}
        path: |
          .cache/poetry
          .cache/pypoetry
    - uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python }}
    - uses: Gr1N/setup-poetry@v7
    - name: Setup virtualenv
      run: poetry install
    - name: Test
      run: make test
    - name: Publish Test Report
      uses: mikepenz/action-junit-report@v2
      if: always()
      with:
        report_paths: 'junit.xml' 
